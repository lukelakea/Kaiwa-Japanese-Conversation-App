{
  "name": "kaiwa",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first Japanese conversation practice app (working title: Kaiwa)",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "uv run --directory backend uvicorn app.main:app --reload --port 8000",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint && npm run lint:backend",
    "lint:backend": "uv run --directory backend ruff check .",
    "format": "npm --prefix frontend run format && npm run format:backend",
    "format:backend": "uv run --directory backend ruff format .",
    "setup": "npm install && npm --prefix frontend install && uv sync --directory backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
